{
  "name": "ios-client",
  "scripts": {
    "lint": "swift-format format -i -r ./**/**/*.swift --configuration swift-format-config.json",
    "lint:check": "swift-format lint -r -s ./**/**/*.swift --configuration swift-format-config.json"
  }
}
